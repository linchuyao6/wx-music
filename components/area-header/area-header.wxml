<view class="header">
    <view class="title">{{title}}</view>
    <view class="hasMore" wx:if="{{hasMore}}" bind:tap="onMoreTap">
        <text class="more">更多</text>
        <van-icon name="arrow" />
    </view>
</view>
